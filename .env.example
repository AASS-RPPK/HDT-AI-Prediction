PORT=8002

# Postgres connection string (dedicated database per microservice)
DATABASE_URL=postgresql+psycopg://postgres:postgres@postgres:5432/hdt_ai_prediction

# Redis URL (Celery broker + result backend)
REDIS_URL=redis://redis:6379/0

# CORS allowed origins (comma-separated). Leave empty to allow all.
CORS_ORIGINS=
